<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddRestaurant"></activity>
        <activity android:name=".ViewRestaurant" />
        <activity android:name=".AdminHome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UpdateFoodActivity" />
        <activity android:name=".EditRestaurantActivity"/>
        <activity android:name=".RestaurantRegisterActivity"/>
        <activity android:name=".RestaurantTableActivity"/>
        <activity android:name=".AddFoodsActivity" />
        <activity android:name=".ViewFoodActivity" />
        <activity android:name=".RestaurantTable" />
    </application>

</manifest>